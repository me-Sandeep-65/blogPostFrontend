import React from "react";
import { CardContainer } from "../components";

export default function MainContainer(){
    return(    
        <CardContainer tailwindClasses="w-full h-full border-white border-2 bg-inherit flex flex-1 flex-col justify-around items-center overflow-hidden">

        </CardContainer>
    );
}